# yaml-language-server: $schema=../question.schema.json
question:
  id: KnowledgeSource
  version: '2026.1'
  title: For each source, how important is it that information you use to operate is easy to verify, such as when its source is accessible?
  type: scale
  required: false
  scale:
    multiple: false
    columns:
      - Very important
      - Important
      - Somewhat important
      - Not very important
      - Least important
  carry_forward:
    from: KnowledgeQ
    include_text_entry: true
