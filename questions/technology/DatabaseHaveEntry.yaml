# yaml-language-server: $schema=../question.schema.json
question:
  id: DatabaseHaveEntry
  version: '2025.1'
  title: Was the database environment you use not listed above?  List below, separated by a comma and a space for each, what **you used in the past year**.
  type: free_text
  required: false
  lines: single
  show_if:
    not:
      DatabaseChoice:
        - no
