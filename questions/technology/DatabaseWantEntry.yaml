# yaml-language-server: $schema=../question.schema.json
question:
  id: DatabaseWantEntry
  version: '2025.1'
  title: Was the database environment you want to use not listed above? List below, separated by a comma and a space for each, what **you want to use next year**.
  type: free_text
  required: false
  lines: single
